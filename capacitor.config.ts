import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sena_ionic_project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
